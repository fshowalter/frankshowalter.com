---
import { getRecentUpdatesProps } from "~/features/recent-updates/getRecentUpdatesProps";
import { RecentUpdates } from "~/features/recent-updates/RecentUpdates";
import AstroPageShell from "~/astro/AstroPageShell.astro";
import { getLayoutBackdropImageProps } from "~/components/Layout/getLayoutBackdropImageProps";
import { Layout } from "~/components/layout/Layout";

const recentUpdatesProps = await getRecentUpdatesProps();
const backdropImageProps = await getLayoutBackdropImageProps("home");

const deck = "I write stuff. Mostly movie reviews, sometimes book reviews.";
const metaDescription =
  "Frank Showalter: writer of stuff. Mainly movie reviews, sometimes book reviews, and all-around swell guy.";
---

<AstroPageShell
  canonical={true}
  meta={{
    description: metaDescription,
  }}
  openGraph={{ description: "Frank Showalter", type: "website" }}
  title="Frank Showalter"
>
  <Layout
    backdrop={{
      backdropImageProps,
      title: "Frank Showalter",
      deck,
    }}
  >
    <RecentUpdates {...recentUpdatesProps} />
  </Layout>
</AstroPageShell>
